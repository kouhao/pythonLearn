python中类的应用：
1、可以多继承；搜索属性从父类继承的深度优先，左到右，而不是搜索两次在同一个类层次结构中，其中有一个重叠；
2、global与nonlocal 语法差异
3、作用域与命名空间
4、类的__init__() 方法是类实例化时，自动调用
5、可变的对象如列表，作为类变量可能带来意外的效果；代码在testClass.py中。
6、类数据属性会覆盖类方法属性，通过命名规范来约束
7、私有变量：只能从对像内部访问的“私有”实例变量，在 Python 中不存在；
以一个下划线开头的命名（例如 _spam ）会被处理为 API 的非公开部分（无论它是一个函数、方法或数据成员）。例子：
8、生成器：Generator 是创建迭代器的简单而强大的工具；需要返回数据时使用yield语句；每次 next() 被调用时，生成器回复它脱离的位置
（它记忆语句最后一次执行的位置和所有的数据值）；例子testGenerator.py

