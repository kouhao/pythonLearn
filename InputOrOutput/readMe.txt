str.rjust():它把字符串输出到一列，并通过向左侧填充空格来使其右对齐
str.ljust():它把字符串输出到一列，并通过向右侧填充空格来使其左对齐
str.center()：它把字符串输出到一列，并通过向左右侧填充空格来使其中对齐
str.format():用法例子1：print('We are the {} who say "{}!"'.format('knights', 'Ni'))
             用法例子2，用keyword：print('We are the {} who say "{}!"'.format('knights', 'Ni'))